import stripHtml from 'string-strip-html';

/**
 * Convert html strings to plain text
 * @param {string} string
 * @returns {string} plain text
 */

const stripHtmlHtmlString = async string => stripHtml(string);

export default stripHtmlHtmlString;
